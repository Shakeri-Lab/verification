{
  "compilerOptions": {
    "target": "ESNext", // Target modern JavaScript
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"], // Crucial: Include ESNext for modern features and DOM
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true, // Enables all strict type-checking options
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "bundler", // Recommended for Vite
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true, // Vite handles the emitting of JS files
    "jsx": "react-jsx", // Essential for React 17+ JSX transform
    "baseUrl": ".", // Important for path aliases
    "paths": {
      "@/*": ["./src/*"] // Assuming your alias is for the src directory
    },
    // "types": ["vite/client"] // Often useful for Vite-specific client types, if not already in tsconfig.node.json or implicitly available
  },
  "include": ["src"], // Only include your app's source code here
  "references": [{ "path": "./tsconfig.node.json" }] // For Vite config, etc.
}
