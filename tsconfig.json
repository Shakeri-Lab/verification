{
  "compilerOptions": {
    // --- Target and Library ---
    "target": "ESNext", // Compile to modern JavaScript
    "lib": ["DOM", "DOM.Iterable", "ESNext"], // CRUCIAL: "DOM" for JSX.Element, "ESNext" for modern JS features

    // --- Modules ---
    "module": "ESNext",
    "moduleResolution": "bundler", // Recommended for Vite and modern bundlers
    "resolveJsonModule": true,     // Allow importing .json files
    "esModuleInterop": true,       // Enables compatibility with CommonJS modules
    "allowSyntheticDefaultImports": true, // Allows default imports from modules with no default export
    "isolatedModules": true,       // Ensures each file can be safely transpiled

    // --- JSX Handling ---
    "jsx": "react-jsx", // CRUCIAL: Use the React 17+ JSX transform

    // --- Strictness and Code Quality ---
    "strict": true, // Enables all strict type-checking options
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true, // Skip type checking of declaration files (d.ts) in node_modules

    // --- Output ---
    "noEmit": true, // Vite handles emitting JavaScript files; tsc is for type checking

    // --- Path Aliases (Optional but common with Vite) ---
    "baseUrl": ".", // Required for "paths" to work
    "paths": {
      "@/*": ["./src/*"] // Example: allows import Button from '@/components/ui/button'
    }
    // "types": ["vite/client"] // Usually helpful for Vite client-side types
  },
  "include": [
    "src" // This tells TypeScript to check all .ts and .tsx files in the 'src' directory
  ],
  "references": [
    // This tells TypeScript that vite.config.ts (and other Node-specific files)
    // are handled by a separate configuration.
    { "path": "./tsconfig.node.json" }
  ]
}
